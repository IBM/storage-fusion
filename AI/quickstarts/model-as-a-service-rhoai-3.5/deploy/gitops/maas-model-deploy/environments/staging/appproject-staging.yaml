apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: fusion-maas-model-deploy-staging
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
  labels:
    environment: staging
    component: model-deploy
spec:
  description: MaaS Model Deploy – Staging Environment

  sourceRepos:
    - 'https://github.com/IBM/storage-fusion.git'

  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'

  roles:
    - name: admin
      description: Admin role for model-deploy staging environment
      policies:
        - p, proj:fusion-maas-model-deploy-staging:admin, applications, *, fusion-maas-model-deploy-staging/*, allow
      groups:
        - maas-admins

    - name: developer
      description: Developer role for model-deploy staging environment
      policies:
        - p, proj:fusion-maas-model-deploy-staging:developer, applications, get, fusion-maas-model-deploy-staging/*, allow
        - p, proj:fusion-maas-model-deploy-staging:developer, applications, sync, fusion-maas-model-deploy-staging/*, allow
      groups:
        - maas-developers

    - name: viewer
      description: Read-only role for model-deploy staging environment
      policies:
        - p, proj:fusion-maas-model-deploy-staging:viewer, applications, get, fusion-maas-model-deploy-staging/*, allow
      groups:
        - maas-viewers

  orphanedResources:
    warn: true
