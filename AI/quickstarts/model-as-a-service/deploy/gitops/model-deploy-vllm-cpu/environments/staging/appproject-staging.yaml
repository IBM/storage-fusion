apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: fusion-vllm-cpu-model-deploy-staging
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
  labels:
    environment: staging
    component: model-deploy-cpu
spec:
  description: vLLM CPU Model Deploy – Staging Environment

  sourceRepos:
    - 'https://github.com/IBM/storage-fusion.git'

  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'

  roles:
    - name: admin
      description: Admin role for vllm-cpu model-deploy staging environment
      policies:
        - p, proj:fusion-vllm-cpu-model-deploy-staging:admin, applications, *, fusion-vllm-cpu-model-deploy-staging/*, allow
      groups:
        - maas-admins

    - name: developer
      description: Developer role for vllm-cpu model-deploy staging environment
      policies:
        - p, proj:fusion-vllm-cpu-model-deploy-staging:developer, applications, get, fusion-vllm-cpu-model-deploy-staging/*, allow
        - p, proj:fusion-vllm-cpu-model-deploy-staging:developer, applications, sync, fusion-vllm-cpu-model-deploy-staging/*, allow
      groups:
        - maas-developers

    - name: viewer
      description: Read-only role for vllm-cpu model-deploy staging environment
      policies:
        - p, proj:fusion-vllm-cpu-model-deploy-staging:viewer, applications, get, fusion-vllm-cpu-model-deploy-staging/*, allow
      groups:
        - maas-viewers

  orphanedResources:
    warn: true
