# Missing CRDs required by ODH Model Controller
# These CRDs are optional but the controller expects them to exist
# Creating them as empty definitions to prevent controller crashes

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: inferencegraphs.serving.kserve.io
  labels:
    app.kubernetes.io/name: llmops-models
    validated-patterns.io/pattern: llmops-platform
spec:
  group: serving.kserve.io
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        x-kubernetes-preserve-unknown-fields: true
  scope: Namespaced
  names:
    plural: inferencegraphs
    singular: inferencegraph
    kind: InferenceGraph
    shortNames:
    - ig

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: llminferenceservices.serving.kserve.io
  labels:
    app.kubernetes.io/name: llmops-models
    validated-patterns.io/pattern: llmops-platform
spec:
  group: serving.kserve.io
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        x-kubernetes-preserve-unknown-fields: true
  scope: Namespaced
  names:
    plural: llminferenceservices
    singular: llminferenceservice
    kind: LLMInferenceService
    shortNames:
    - llmis

