apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: granite-llm
  # Namespace will be set by Kustomize from Application CR's destination.namespace
  # Labels (app.kubernetes.io/name, validated-patterns.io/pattern) will be automatically added by commonLabels in kustomization.yaml
  # Keep resource-specific labels below for model identification
  labels:
    app: llm-serving
    model: granite
    version: "1.0"
  annotations:
    # Ensure this is created after ConfigMap
    argocd.argoproj.io/sync-wave: "0"
spec:
  predictor:
    serviceAccountName: default
    containers:
    - name: kserve-container
      # Using vLLM which supports Hugging Face models and OpenAI-compatible API
      # Publicly available image, no pull secret needed
      image: vllm/vllm-openai:latest
      imagePullPolicy: Always
      resources:
        limits:
          nvidia.com/gpu: "1"
          memory: "16Gi"
          cpu: "4"
        requests:
          nvidia.com/gpu: "1"
          memory: "16Gi"
          cpu: "4"
      env:
      # Load model from Hugging Face Hub - model name comes from ConfigMap
      - name: MODEL
        valueFrom:
          configMapKeyRef:
            name: model-config
            key: MODEL_NAME
      - name: HF_HOME
        value: "/tmp/hf_cache"  # Use writable directory for Hugging Face cache
      - name: VLLM_CACHE_ROOT
        value: "/tmp/vllm_cache"  # Use writable directory for vLLM cache
      - name: HOME
        value: "/tmp"  # Set home to writable directory
      # vLLM uses OpenAI-compatible API format
      args:
      - --model
      - $(MODEL)  # Reference the MODEL environment variable
      - --host
      - "0.0.0.0"
      - --port
      - "8080"
      - --api-key
      - "EMPTY"  # vLLM requires API key, but we can use a dummy value
      - --trust-remote-code  # Required for some models
      - --gpu-memory-utilization
      - "0.75"  # Use 75% of GPU memory instead of default 90% to avoid memory errors

