---
# ============================================================================
# Ministral 3 8B Instruct Model Serving Application - GitOps Configuration
# ============================================================================
# Configuration for deploying Mistral AI's Ministral 3 8B Instruct model
#
# Model: https://huggingface.co/mistralai/Ministral-3-8B-Instruct-2512
# This is an open-source model that does NOT require HuggingFace authentication
#
# RESOURCE REQUIREMENTS:
# - 1 GPU (A100, H100, or similar)
# - 16Gi memory
# - Deployment time: ~5-10 minutes
# ============================================================================
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ministral-3-8b-model
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/name: ministral-3-8b-model
    validated-patterns.io/pattern: llmops-platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    # IMPORTANT: Replace <your-username> with your GitHub username after forking the repository
    repoURL: https://github.com/<your-username>/storage-fusion.git
    targetRevision: main
    path: AI/fusion-model-serving/deploy/gitops/cluster
    kustomize:
      patches:
        # Patch 1: Override commonLabels in Kustomization
        - target:
            kind: Kustomization
          patch: |-
            - op: replace
              path: /commonLabels/app.kubernetes.io~1name
              value: ministral-3-8b-model
            - op: replace
              path: /commonLabels/validated-patterns.io~1pattern
              value: llmops-platform

        # Patch 2: Configure InferenceService for Ministral 3 8B
        # This is an open-source model - no HF_TOKEN needed
        - target:
            kind: InferenceService
          patch: |-
            - op: replace
              path: /metadata/name
              value: ministral-3-8b-instruct
            - op: replace
              path: /metadata/labels/model
              value: ministral
            - op: replace
              path: /spec/predictor/containers/0/env/0/value
              value: mistralai/Ministral-3-8B-Instruct-2512
            - op: replace
              path: /spec/predictor/containers/0/resources/limits/nvidia.com~1gpu
              value: "1"
            - op: replace
              path: /spec/predictor/containers/0/resources/limits/memory
              value: "16Gi"
            - op: replace
              path: /spec/predictor/containers/0/resources/requests/nvidia.com~1gpu
              value: "1"
            - op: replace
              path: /spec/predictor/containers/0/resources/requests/memory
              value: "16Gi"
            - op: add
              path: /spec/predictor/containers/0/args/-
              value: "--max-model-len"
            - op: add
              path: /spec/predictor/containers/0/args/-
              value: "32768"

  destination:
    server: https://kubernetes.default.svc
    # Deploy to model-serving namespace
    namespace: model-serving

  # Ignore diff on shared resources
  ignoreDifferences:
    - group: rbac.authorization.k8s.io
      kind: Role
      name: argocd-manager
      jsonPointers:
        - /metadata/annotations/argocd.argoproj.io~1tracking-id
        - /metadata/labels
    - group: rbac.authorization.k8s.io
      kind: RoleBinding
      name: argocd-manager
      jsonPointers:
        - /metadata/annotations/argocd.argoproj.io~1tracking-id
        - /metadata/labels
    - group: ""
      kind: ConfigMap
      name: inferenceservice-config
      jsonPointers:
        - /metadata/annotations/argocd.argoproj.io~1tracking-id
        - /metadata/labels

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

