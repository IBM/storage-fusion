# Core dependencies — same as requirements.txt, minus streamlit
requests>=2.31.0
pyyaml>=6.0
python-dotenv>=1.0.0

# FastAPI + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Async HTTP client — used by the CPU model router proxy
httpx>=0.27.0

# Monitoring (optional)
prometheus-client>=0.19.0
